"use client";

function users() {
  return (
    <div>
      {/* {users.map((user) => (
        <div key={user.id}>
          <h1>{user.name}</h1>
          <h1>{user.email}</h1>
          <h1>{user.role}</h1>
        </div>
      ))} */}
    </div>
  );
}

export default users;
